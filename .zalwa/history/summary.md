2026-07-26 | #51 — feat: cells are always drawn at full size | The Cell Size slider is gone and Cells are drawn at the full lattice spacing, bought for one row of panel height after #37, #30 and #42 each fought the same shortage. The removal was only defensible because **the PRD's own prose overstated what it cost**: B6 promised "solid sheets" and the Depth Window section warned of an "opaque brick", both written as if the Grid were dense, while the measured population is 3–7% live and nearer 2% on a Pattern run — so full size produces chunky connected clusters, not filled Layers. Checking the measured number against the prose is what made the issue possible; taking the document at its word would have read the change as a regression. Full size turned out to be the *absence* of a factor rather than a factor of 1.0, because the geometry was already a cube of `CELL_SPACING` — so the shader edit deleted `* uCellSize` instead of pinning a constant, and a future reader looking for a `CELL_SIZE` will not find one. **The work deliberately ran past its acceptance criteria** to two clauses they did not name — the Viewer responsibility bullet in Actors, and `stack.md`'s Interface row and architecture sentence — which are precisely the stranded-clause failure #48 exists to catch, and which this issue's own note on reversibility cites. Two costs are recorded rather than mitigated: the floating-point-tail test was deleted rather than rehomed because `cellSize` was the only fractional-step bound, leaving `decimalPlaces()` and `toFixed()` correct but unexercised and the guard needing rewriting if a fractional setting returns; and the height bought is already spent — portrait now fits at `contentHeight` 516 of `visibleHeight` 516, zero headroom, so the next control added overflows again. B6's numbering gap was left open on purpose, because renumbering would strand every citation in closed issues and history. | .zalwa/history/2026/07/26/issue-51-cells-full-size.md
2026-07-26 | #50 — feat: a chosen pattern can be shared by URL | A chosen Pattern now travels as `?pattern=<id>`, so a pasted link opens on what the sender was watching rather than a random soup — the product's **first externally-controlled input**, which is the frame the whole session turns on. Every prior security review had concluded "no attacker-controlled surface exists"; true then, false now, so this one was briefed explicitly not to inherit it. A review is only as good as the threat model it is handed — a reviewer told the product has no inputs will confirm it, because that sentence is a premise rather than a finding. It came back clean, and the one finding that could have been real is instructive: `patterns.find(p => p.id === requested)` compares a *value*, so `?pattern=__proto__` is inert, whereas `patterns[requested]` would have made it a live question — the safe shape was chosen for the stable-id requirement and got safety as a by-product, so anyone later optimising the scan into a map lookup re-opens it without noticing. A throwaway browser check found a defect **unit tests structurally could not**: a bad parameter degraded correctly but stayed in the address bar, so the URL claimed a Pattern the Run was not using; neither pure function was wrong, the defect lived in the wiring between them. The same script then produced a false alarm — a prior page's rAF WebGL loop starving the next navigation, presenting as a hang on Pattern URLs — and diagnosing that mattered more than the check: **a failing check against a new feature is not evidence the feature is broken.** `stack.md`'s throw-loudly convention got its first exception, reasoned in `share.ts` rather than a commit message, because a shared link rendering a stack trace destroys the one moment it exists for. And the Actors justification was **replaced rather than patched** — a shared link means somebody does choose what a first-time visitor sees — leaving the line the actor list now rests on: choosing among what exists is not authorship; creating what others see is. | .zalwa/history/2026/07/26/issue-50-share-pattern-by-url.md
2026-07-26 | #49 — feat: the pattern list grows to eight | Six more Patterns beside Gosper's gun — pulsar, pentadecathlon, Kok's galaxy, queen bee shuttle, R-pentomino, acorn — plus a stable `id` per Pattern. The session's substance is that **a test caught a Pattern that was simply wrong**: the queen bee shuttle as first transcribed had 23 Cells and no period. Nothing else would have found it, because a mistranscribed Pattern still renders and still looks like something — what a wrong arrangement cannot do is keep a period. Each oscillator is advanced exactly one period and compared against Generation 0, *and separately asserted not to return early*, which is the half that matters: without it a still life passes trivially. Equally reusable: three failures had three different causes and only one was the data — Kok's galaxy failed a Cell count I asserted from memory, the acorn failed a threshold derived from published *unbounded* figures that the Bounded Edge makes unreachable, and only the shuttle was a real transcription error. Reading which kind a failure is, before editing anything, is the skill. The security review noticed unprompted that `id` was **write-only** — added for #50 while the chooser still keyed on array index, leaving two ways to name a Pattern — and wiring it up removed a bug class rather than guarding it, since matching on id makes #46's `Number("") === 0` placeholder hazard impossible by construction. Two Patterns were deliberately not shipped, with reasons recorded beside PATTERNS rather than only in the issue: a lone spaceship leaves the Grid permanently empty, and Simkin's gun is period 120 against a default Depth of 60. | .zalwa/history/2026/07/26/issue-49-pattern-list-grows.md
2026-07-26 | #46 — feat: choosing a pattern switches the explosion off | Choosing a Pattern now switches the Explosion off as part of starting that Run, so Gosper's gun no longer runs perfectly for two hundred Generations and then blows itself apart — which read as the product breaking rather than as a rule worth watching. The session's real content is that **the persona gate failed at QA and was right to**: the first implementation was three lines inside the rAF loop in `main.ts`, a module that imports three.js and no unit test can reach, so the feature had zero coverage — and the tests written alongside it *looked* like coverage while exercising the Simulation, which already behaved correctly. Graded D, returned to step 6, extracted the rule to `applyStartRule` in `settings.ts` and covered it properly. The reusable lesson: "I added tests" is not "the change is tested", and the question separating them is which module the new behaviour actually lives in — where a rule lives is a testability decision, not a tidiness one. Two smaller ones: an AC was already satisfied by #42's existing 300-Generation test, so checking beat writing and the new test went to the untested opt-back-in path instead; and the AC requiring the switch to visibly move cost no code at all, because `restartRun()` already calls `panel.refresh()` — the only change worth making was to the comment that would have invited a future session to narrow that call and break it silently. The asymmetry is deliberate and its cost is stated in B8: Random does not restore the Explosion, so a Viewer who tries a Pattern and then presses Random inherits a decaying Run. | .zalwa/history/2026/07/26/issue-46-pattern-switches-explosion-off.md
2026-07-26 | #42 — feat: choose a starting pattern instead of a random seed | A Pattern dropdown shipping with Gosper's glider gun sits beside a restart control relabelled Random; both start a fresh Run and differ only in what Generation 0 holds. The sharpest lesson is that the issue's own framing was wrong and a test caught it: both it and my first draft called the gun's reflector blocks "stationary blocks whose cells never change state", but a reflector is a **catalyst, not a still life** — the shuttle disturbs each block's inner face every cycle and it reforms, so four Cells age continuously rather than eight. "These cells never change" is a claim about a pattern's *dynamics*, not its initial arrangement, and only simulation settles it. The gun is verified as a working gun rather than as data, by arithmetic that a transcription error could not survive: population 86 on an unbounded Grid at Generation 300 is exactly 36 + 10 gliders × 5, i.e. period 30, and 56 on the bounded Grid at both 300 and 600 is the steady state of gliders leaving as fast as the boundary destroys them. The Grid floor moved to 50 rather than adding a refusing control, which deletes the too-small case instead of handling it — at the cost of small Grids entirely, and with floor equal to default both sliders now start pinned at minimum. Two smaller lessons: the predicted panel-height problem did not exist, because a control added *beside* something costs no height while one added *below* does; and `Number("")` is 0, which the security review called unreachable and keyboard arrow-keys made reachable, silently starting a Run with the first Pattern. | .zalwa/history/2026/07/26/issue-42-starting-pattern.md
2026-07-26 | #30 — feat: turn death by old age off | Filed as a control suppressing Death by Old Age and shipped as something else entirely: reaching Maximum Age no longer kills, it detonates — scattering life across the Cell's own position and every in-grid neighbour at age 1, so a capped Cell survives its own burst and a cluster resets whole rather than leaving a hole. The operator redirected the design at the plan gate, which was the cheapest moment to resolve an ambiguity between two materially different rules. Three things follow and each is the reusable part: the neighbour-at-cap exclusion was dropped because it became *provably unobservable* once a capped Cell revives its own position, not because it was inconvenient — and the first analysis wrongly offered keeping it as the conservative choice; `.zalwa/principles.md` principle 6 was revised *before* the contradicting code was written (PR #43), since its naming instruction inverted once nothing dies of age, and routing it was itself a correction — project principles belong to zalwa-reflect, not zalwa-meditate; and the population figures were re-measured rather than inherited, because a rule injecting nine Cells per exploder instead of eight invalidates every density number written against the old one (3–7% live at the default, 11–21% at A=4, so no runaway). A 44px switch pushed the portrait sheet off-screen — the #10 failure of a control technically large and actually harder to reach — so it takes the slider minimum, and the smoke threshold change is recorded rather than buried. The accessible-name defect was caught by the persona gate alone, with typecheck, lint, 105 tests, and the smoke check all green while it was present. | .zalwa/history/2026/07/26/issue-30-death-by-old-age-control.md
2026-07-26 | #29 — feat: the run starts at 10 generations per second | Filed at 15/s and shipped at 10/s, revised at pick time on the reasoning that legibility beats immediacy — past some rate a Layer is on screen too briefly to read as a shape and the eye takes the whole thing as motion, which works against the reason the third axis is time. What it buys is the first Explosion arriving around twenty-one seconds instead of twenty-seven, since Maximum Age is pinned at 200 and nothing can detonate before Generation 214. Two documents were left asserting "roughly half a minute" — a figure derived from the old default — which is the reusable lesson: prose stating a *duration* rather than a *generation count* is coupled to Speed and must be re-derived whenever Speed moves. The default is now pinned by a test AC 3 did not ask for, accepting that future tuning edits a test line, because a default whose exact value carries an argument should not move silently. The honest limit: the legibility claim is temporal, so no still frame or headless check can substantiate it — 8/s and 10/s produce identical frames — and both UX and Product graded B on exactly that. | .zalwa/history/2026/07/26/issue-29-ten-generations-per-second.md
2026-07-26 | #37 — fix: the control sheet does not fit an iPhone | Reported from a real iPhone within minutes of deploying #11, and both defects were real despite the layout having passed a purpose-built Playwright harness, 99 unit tests, a security review, and a persona gate. 44px targets were right per control and wrong for the interface: six of them plus separation needed 852px in a 664px viewport, so every control was harder to reach for being below the fold — sliders dropped to 32px on the reasoning that a slider is dragged rather than tapped, while tapped-once controls kept 44px. The toggle was cut off because `position: fixed` resolves against the *layout* viewport, which on iOS Safari is the large one, so it sat behind the browser toolbar; both controls now live in a fixed `100dvh` layer carrying the safe-area insets, where `bottom` means the visible bottom by construction. The harness had asserted the sheet's *box* fits the viewport — an assertion `max-height` guarantees can never fail — instead of asserting the content fits, which is the reusable lesson: a check that cannot fail reads as coverage and is worse than none. | .zalwa/history/2026/07/26/issue-37-sheet-fits-iphone.md
2026-07-26 | #11 — feat: work on a phone | The panel became a dismissable sheet on a small viewport, collapsed on arrival so a phone shows the Structure rather than a stack of sliders, with every control cleared to 44px on a coarse pointer and the navigation hint rewritten for the input the Viewer actually has. The breakpoint lives only in the stylesheet — the panel holds one boolean and writes one class — so a rotation needs no listener and the desktop arrangement is provably untouched. The entry worth reading is why nothing is animated: the first version transitioned the sheet's appearance and it never opened, because a transition only runs once the compositor assigns a start time and this page withholds it indefinitely under a WebGL canvas and a `backdrop-filter` surface — `getAnimations()` reported every property `running` with `startTime: null`, so the panel held its closed values forever and the tap looked ignored. Two more defects came from the same harness and nothing else: `max-height` was bounding the content box, so the panel overran its own cap by its padding on desktop too, and flex-shrink collapsed the dismiss control to the 16px of its icon. All three passed typecheck, lint, and 99 unit tests. | .zalwa/history/2026/07/26/issue-11-work-on-a-phone.md
2026-07-26 | #32 — feat: sign the panel with a link to the author's GitHub | The product gained a signature and its only outbound link. The supplied image turned out to be Magritte's painting with the background removed — flagged before the issue was written rather than at deploy time, and replaced with original geometry, because copying the idea of a bowler hat and an apple is fine and copying his rendering of it is not. The artwork was drawn twice and corrected a third time: monoline strokes matched the panel's hairlines and dissolved into an unreadable glyph at 3rem, and even the silhouette needed fixing once blown up to judge. Every acceptance criterion passed against the illegible first draft, which is the sharpest instance yet of what #25 owns — `getBoundingClientRect` cannot see that something looks wrong. New tab is a requirement rather than a convention, because navigating away discards the run. | .zalwa/history/2026/07/26/issue-32-panel-signature.md
2026-07-26 | #26 — feat: a cell dying of old age explodes into its neighbours | Death by Old Age now scatters life into every in-bounds neighbour at age 1, so the disturbance the PRD always claimed the age cap was for became something a Viewer sees rather than infers. The literal request — every death explodes — was modelled first and rejected on the numbers: it saturates the Grid to 76% within three Generations and collapses the palette to one colour. Implemented as a second pass reading only the previous Generation, with one shared `diesOfOldAge` trigger so the two passes cannot disagree, and Cells at the cap deliberately not reviving each other so a cluster leaves a hole rather than resetting itself. Maximum Age moved to 200 as both ceiling and default — knowingly against the PRD's own anti-stasis reasoning, reaffirmed by the operator, and recorded in three places so it reads as intent. The cost was reported rather than hidden: at A=200 the structure renders near-monochrome, which works against B11 and is routed as a follow-up. | .zalwa/history/2026/07/26/issue-26-old-age-explosion.md
2026-07-26 | #10 — feat: set grid dimensions and restart a run | Grid dimensions became a staged Viewer control (16–96) and Restart became reachable, with the pending state signposted on both the readout and the button because a change that silently waits is indistinguishable from one that failed. The Run collapsed into one object with a single start path, since starting one had grown to six separate resets and forgetting any of them fails in ways that trace back poorly. The Grid ceiling was chosen by the operator on the instance arithmetic — 96 × 96 × 120 is 1.1M instances, four times anything shipped and still unmeasured on real hardware, which #12 owns. Verification caught a defect this issue created: the new Restart button fell off the bottom of a fixed panel with nothing to scroll. | .zalwa/history/2026/07/26/issue-10-grid-and-restart.md
2026-07-25 | #9 — feat: control speed, depth, maximum age, and cell size while running | The product became adjustable: a hand-built panel over native range inputs, with Cell Size moved from baked geometry into a shader uniform and the instance ring allocated once at the panel's Depth Window ceiling so a slider drag allocates nothing. The Depth Window travels rather than snapping, because the shader's fade already meets its own cut-off exactly — so the Layers a narrowed window gives up dissolve for free rather than popping. Two bugs found by reading the relay path, not by tests: a batched rewrite would have uploaded only its last Layer, and placing Layers against the Stack's held depth would have dropped a narrowed structure 40 units the moment its fade completed. | .zalwa/history/2026/07/25/issue-9-live-controls.md
2026-07-25 | #7 — feat: navigate the structure by pointer and touch | Attached OrbitControls with damping, an unclamped polar angle so the underside is reachable, and distance limits derived from the structure's extent. Surfaced a defect shipped in #8 that only existed outside the range #8 could exercise: the cell edge swallowed cells whole once they shrank to pixel size, so zooming out made the structure vanish. AC 5 proven by comparing still-camera frames rather than adding a diagnostic hook to production. | .zalwa/history/2026/07/25/issue-7-navigate.md
2026-07-25 | #8 — feat: encode cell age as colour and history depth as fade | Colour now carries age and depth carries a dissolve, with cells made cubic and edge-drawn per operator addition. The linear age-to-colour map satisfied the acceptance criterion on paper while painting everything one colour, because Life skews cell ages young — fixed by curving the mapping without moving either endpoint. Fade mixes toward the background rather than using alpha, so 138,000 unsorted instances stay correctly depth-sorted. | .zalwa/history/2026/07/25/issue-8-colour-and-fade.md
2026-07-25 | #6 — feat: show the accumulated structure in three dimensions | The product became visible: one instanced draw for the whole stack, with height, visibility, and placement derived in the vertex shader from two uniforms, so per-frame CPU work is constant. Shipped a fused blue mass on the first render that passed every automated check — found only by screenshotting it, fixed by separating cell height from layer spacing, and `pnpm smoke` committed so the check repeats. Smoothness left explicitly unverified: software rasterisation says nothing about real hardware. | .zalwa/history/2026/07/25/issue-6-render-structure.md
2026-07-25 | #5 — feat: retain a bounded window of history | Built LayerStack as a ring buffer so retirement is an overwrite and held memory is constant by construction; Layer generation derived from depth rather than stored; Simulation now pushes each generation and clears on restart. Resolved a real tension between AC 1 and AC 4 by freezing the seed as Generation 0's layer, so a fresh stack holds one layer rather than zero. 24 new tests, 53 total. | .zalwa/history/2026/07/25/issue-5-history-window.md
2026-07-25 | #4 — feat: compute generations under the Life rule | Built the simulation core: Grid with age-as-state in a Uint16Array, B3/S23 plus Death by Old Age, and Run state with double buffering and an injectable random source. 29 tests including golden Life patterns, which caught a fixture-mutating bug in the test helper on the first run. All five acceptance criteria met. | .zalwa/history/2026/07/25/issue-4-life-rule.md
2026-07-25 | #3 — feat: serve a deployable page at a public URL | Established the toolchain (TypeScript strict + noUncheckedIndexedAccess, Vite, pnpm, Biome pinned exact, Vitest) and deployed static assets to Cloudflare Workers, verified live. Switched from #4 mid-session after finding it needed a test runner that did not exist yet. CI and deploy-on-merge deferred by operator decision; two acceptance criteria recorded unmet with a follow-up filed. | .zalwa/history/2026/07/25/issue-3-deployable-page.md
