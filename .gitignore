node_modules/
dist/
.wrangler/
*.local
.DS_Store

# Operator scratch. Findings written here for copying into zalwa's own notes —
# deliberately outside the repo's history, because they are about the zalwa
# system rather than about this product.
sandbox.txt

# Reference material only — never shipped, never committed.
#
# The author's mark (see the PRD's Author credit) is an original drawing of the
# idea behind Magritte's "The Son of Man". The painting itself is under copyright
# and lives here as a look-at reference while drawing. Ignoring the directory is
# what stops it being committed by an absent-minded `git add -A` and ending up in
# a public repo and a deployed bundle.
assets/
